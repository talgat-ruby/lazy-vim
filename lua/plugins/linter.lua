return {
  "mfussenegger/nvim-lint",
  opts = {
    linters = {
      -- sqlfluff = {
      --   root_dir = require("lspconfig.util").root_pattern(".sqlfluff"),
      --   args = {
      --     "lint",
      --     "--format",
      --     "json",
      --   },
      -- },
    },
  },
}
